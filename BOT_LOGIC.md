# Логика работы HypeBot (Модуль SetupFinder)

Этот документ описывает алгоритмы и принципы работы автоматического сканера торговых сетапов, внедренного в бота.

## 🚀 Как это работает

Бот работает в двух режимах одновременно:
1.  **AI-аналитик (по запросу):** Пользователь отправляет тикер (например, `ETH`), и бот проводит глубокий анализ волн Эллиотта и Вайкоффа с помощью LLM (Gemini/DeepSeek).
2.  **Автоматический Сканер (фоновый режим):** Бот каждую минуту проверяет рынок на наличие четких технических сетапов и присылает уведомления.

### Мониторинг
Сканер автоматически следит за следующими парами на Hyperliquid:
- **ETH**
- **BTC**
- **LTC**
- **SOL**

Таймфрейм анализа: **5 минут (5m)**.

---

## 📊 Техническая логика (Индикаторы)

Решения принимаются на основе жесткой математической логики (без участия AI) для максимальной скорости и надежности. Использются индикаторы, описанные в `SETUPS.md`.

### Используемые индикаторы
1.  **VWAP (Volume Weighted Average Price):**
    - Средневзвешенная цена по объему. Используется как главный определитель тренда.
    - *Выше VWAP* = приоритет Лонг.
    - *Ниже VWAP* = приоритет Шорт.
2.  **EMA (Экспоненциальные скользящие средние):**
    - **EMA 9 (Быстрая)** и **EMA 21 (Медленная)**.
    - Пересечение и расположение относительно друг друга подтверждает тренд.
3.  **RSI (Relative Strength Index):**
    - Период: **9**.
    - Используется для поиска точек входа (откат) или перекупленности/перепроданности (разворот).

---

## 🎯 Реализованные Сетапы

Бот ищет три типа ситуаций:

### 1. Pullback (Откат по тренду) — Низкий риск
Самый надежный сетап. Вход в сторону тренда после коррекции.

*   **Условия для ЛОНГ:**
    *   Цена > VWAP (Глобальный аптренд).
    *   EMA 9 > EMA 21 (Локальный аптренд).
    *   Цена коснулась EMA 21 (поддержка) и отскочила вверх.
    *   RSI в "здоровой" зоне (45-65), не перекуплен.
*   **Условия для ШОРТ:**
    *   Цена < VWAP.
    *   EMA 9 < EMA 21.
    *   Цена коснулась EMA 21 снизу (сопротивление) и отскочила вниз.

### 2. Breakout (Импульсный пробой) — Высокий риск
Агрессивный вход на сильном движении.

*   **Условия:**
    *   Резкий скачок объема (на 20% выше предыдущей свечи).
    *   Большое тело свечи (в 1.5 раза больше предыдущей).
    *   Цена пробивает VWAP или идет по тренду.
    *   RSI > 60 (для Лонга) или < 40 (для Шорта), показывая силу импульса.

### 3. Reversion (Возврат к среднему / Контртренд) — Средний риск
Попытка поймать разворот, когда цена слишком сильно отклонилась.

*   **Условия:**
    *   Цена отклонилась от VWAP более чем на **2%**.
    *   RSI в экстремальной зоне: < 30 (для Лонга) или > 70 (для Шорта).
    *   Расчет на то, что цена вернется к VWAP как к магниту.

---

## 🛠 Установка и Запуск

### Предварительные требования
*   Python 3.10+
*   Linux/macOS (рекомендуется)

### 1. Установка зависимостей
```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate

# Установите библиотеки
pip install -r requirements.txt
```

### 2. Настройка
В файле `.env` укажите токен вашего бота:
```
TG_TOKEN=ваш_телеграм_токен
IS_GEMINI=False (или True)
```

### 3. Запуск
```bash
python main.py
```
Бот запустится и выведет сообщение `🚀 Запущен сканер рынка...`.

### 4. Использование
1.  Найдите бота в Telegram.
2.  Нажмите **/start**.
3.  Теперь вы подписаны на сигналы. Как только сканер найдет ситуацию на бирже, вы получите сообщение.
