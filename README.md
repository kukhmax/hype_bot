# 🤖 HypeBot: AI Trading Assistant для Hyperliquid

**HypeBot** — это интеллектуальный торговый терминал в Telegram, который использует **Google Gemini 1.5 Pro** для технического анализа крипторынка (Волны Эллиотта, Вайкофф) и помогает совершать сделки на децентрализованной бирже **Hyperliquid**.

---

## 🚀 Возможности

*   **AI-Аналитика:** Анализирует структуру рынка, определяет тренд, ищет паттерны разворота и выдает прогноз с Confidence Score (уровнем уверенности).
*   **Визуализация:** Генерирует график свечей (mplfinance) с нанесенными зонами интереса прямо в Telegram.
*   **Безопасность:** Использует систему **Agent Wallet**. Ваш основной приватный ключ **никогда** не используется ботом.
*   **Smart Entry:** (В разработке) Выставление ордеров с автоматическим расчетом Stop Loss и Take Profit.

---

## 🛠 Установка

### Требования
*   Python 3.10+
*   Аккаунт на Hyperliquid (с депозитом в USDC на Arbitrum/Hyperliquid L1)
*   API ключ Google Gemini (AI Studio)
*   Telegram Bot Token

### Шаг 1. Клонирование репозитория
```bash
git clone https://github.com/your-repo/Hype_bot.git
cd Hype_bot
```

### Шаг 2. Виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# venv\Scripts\activate   # Для Windows
```

### Шаг 3. Установка зависимостей
```bash
pip install hyperliquid-python-sdk eth-account python-dotenv aiogram google-generativeai pandas mplfinance aiosqlite matplotlib
```

---

## ⚙️ Настройка

### 1. Переменные окружения
Создайте файл `.env` на основе примера:
```bash
cp .env.example .env
```
Заполните его:
*   `TG_TOKEN`: Получите у @BotFather.
*   `GEMINI_API_KEY`: Получите в [Google AI Studio](https://aistudio.google.com/).
*   `MAIN_WALLET_ADDRESS`: Публичный адрес вашего кошелька (Arbitrum), с которого торгуете на Hyperliquid.

### 2. Настройка Agent Wallet (Критически важно!)
Бот торгует через "Агента" — отдельный кошелек, которому вы даете право только торговать (без вывода средств).

1.  Запустите скрипт генерации:
    ```bash
    python3 setup_agent.py
    ```
2.  Скопируйте появившийся **Address** агента.
3.  Перейдите на [Hyperliquid Trade](https://app.hyperliquid.xyz/trade).
4.  Подключите свой основной кошелек.
5.  Зайдите в настройки (API / Manage Agent) и добавьте скопированный адрес как нового агента. Подпишите транзакцию основным кошельком.
6.  Скопируйте **Private Key** (из вывода скрипта `setup_agent.py`) и вставьте его в файл `.env` в поле `AGENT_PRIVATE_KEY`.

---

## 🖥 Использование

Запустите бота:
```bash
python3 main.py
```

### Команды в Telegram:
*   `/start` — Приветствие и проверка статуса.
*   **Текст (например, `ETH`, `BTC`, `SOL`)** — Бот начнет анализ пары:
    1.  Скачивает последние 200 свечей с Hyperliquid.
    2.  Считает индикаторы (RSI, ZigZag, Volume).
    3.  Отправляет данные в Gemini AI.
    4.  Присылает график и торговый план (Long/Short, уровни входа и выхода).

---

## 📂 Структура проекта

```text
Hype_bot/
├── services/
│   ├── ai_analyst.py   # Логика общения с Gemini (Промпты, JSON)
│   ├── charts.py       # Генерация картинок графиков (mplfinance)
│   ├── indicators.py   # Расчет RSI, ZigZag, SMA
│   ├── market_data.py  # Загрузка свечей через Hyperliquid SDK
│   └── trading.py      # (Beta) Исполнение ордеров через Agent Wallet
├── main.py             # Точка входа (Telegram Bot)
├── setup_agent.py      # Скрипт создания ключей
├── utils.py            # Математические утилиты
└── trade_bot.db        # SQLite база данных (история)
```

## ⚠️ Дисклеймер
Этот бот является помощником и предоставляет аналитику на основе ИИ. ИИ может галлюцинировать. Все торговые решения вы принимаете на свой страх и риск. Разработчик не несет ответственности за финансовые потери.
